'use strict';

/**
 * directorio-org controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::directorio-org.directorio-org');
